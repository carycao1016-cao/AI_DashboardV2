# 19 变更日志：识别结果持久化

## 本次范围

- 在识别任务结果中保存 Layer 1 Outline 响应、Layer 2 `TableBoundaryProposal` 和 Python `BoundaryValidationResult`。
- 新增按文件版本读取识别结果的接口，供 Data Explorer 接入。
- 保存内容仍是结构和状态元数据，不把业务单元格值复制到任务结果中。

## 测试边界

本次通过前端构建、后端导入和离线 Parser 测试验证，没有新增 DeepSeek 请求。
