# Golden 标注显著性扩展

**日期：** 2026 年 8 月 16 日  
**状态：** 基于源文件的标注更新；原始用户模板已保留

## 新增内容

- 新增 Quantum `Tabs_%95.xlsx` 显著性 Sheet 的 `G004` 工作簿记录。
- 新增 Decipher `Percentages_Sig1` 的 10 张物理表标注（`G002_SIG_T01` 至 `G002_SIG_T10`）。
- 新增一张 Quantum “下一行显著性”物理表标注（`G004_SIG_T01`）。
- 在 `Table_Annotation` 中增加显著性来源 Sheet、数值区域、显著性标记区域和代码表头区域字段。
- 在 `Header_Annotation` 中增加来源 Sheet、数值列和标记列字段。
- 在 `Cell_Truth` 中增加来源 Sheet 和显著性标记单元格的来源信息。
- 增加 Decipher 的 24 个表头代码映射，以及 Quantum 的 27 个表头代码映射。
- 增加两种显著性布局的源文件样例，并保留 `-` 与数字 0 的区别。

## 已覆盖的布局

- Decipher：百分比值位于交替列，显著性代码位于相邻列。
- Quantum：百分比值之后，在同一列的下一行放置显著性代码。

## 输出

更新后的模板已归档到最终 Golden 工作簿中。原始 `PoC/Golden_Annotation_Template.xlsx` 没有被覆盖。
