# 23 变更日志：项目名称唯一性

## 决策

- 当前本地 MVP 工作区内项目名称必须唯一。
- 重名判断会统一 Unicode 全角/半角、大小写和连续空白。
- 界面只显示项目名称；`project_id` 仅作为后端和审计标识。
- 历史上已经存在的重名项目保留，不自动删除。

## 后续用户范围

接入登录和工作区后，唯一性应改为 `(workspace_id, normalized_project_name)`，而不是全局唯一。

## 验证结果

已有项目的全角、大小写混合和额外空白变体均返回 HTTP 409，没有新增模型请求。
